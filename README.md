# Simple Compile Time TypeList

## Features:
- std::list like compile time operations in the form of `type_traits`.

## Motivation:
- Just for fun.

## Usage:
- Use `make build-debug` to build project
- Check tests/type_list_test.cpp
